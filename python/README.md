# Python module

Level 2 challenges: ETL, pandas, pytest.

```bash
cd python
pip install -r requirements.txt
pytest tests/ -v
```

Implement `challenge_2_1_load.py` first — tests are written but will fail until you code.
